


**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Create a `.env.local` file in the project root and set `GROQ_API_KEY` to your Groq API key
3. Run the app:
   `npm run dev`
